#include <stdint.h>
#include <stdio.h>
#include "kernel_functions.h"

enum exception init_kernal(void);
enum exception create_task(void);
void run(void);
void terminate(void);

int main()
{

  return 0;
}
